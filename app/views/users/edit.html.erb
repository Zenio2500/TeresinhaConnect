<div class="mb-4" style="display: flex; justify-content: space-between; align-items: center;">
  <div>
    <h1>Editar Usuário</h1>
    <p style="color: var(--gray-600);">Atualizar informações do usuário <%= @user.name %></p>
  </div>
  <button type="button" class="btn btn-danger" style="height: fit-content;" onclick="showDeleteModal()">
    🗑️ Excluir Conta
  </button>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", user: @user %>
  </div>
</div>

<!-- Modal de Confirmação de Exclusão -->
<div id="delete-account-modal" class="modal-overlay" style="display: none;">
  <div class="confirmation-modal">
    <div class="modal-header">
      <span class="modal-icon danger">🗑️</span>
      <h3 class="modal-title">Confirmar Exclusão de Conta</h3>
    </div>
    <div class="modal-body">
      <p><strong>⚠️ ATENÇÃO:</strong> Tem certeza que deseja excluir sua conta permanentemente?</p>
      <p>Esta ação não pode ser desfeita e todos os seus dados serão removidos do sistema, incluindo:</p>
      <ul style="margin-left: 20px; margin-top: 10px;">
        <li>Informações pessoais</li>
        <li>Histórico de atividades</li>
        <li>Associações com pastorais</li>
        <li>Escalas de leitura</li>
      </ul>
    </div>
    <div class="modal-actions">
      <button class="btn btn-secondary" onclick="closeDeleteModal()">Cancelar</button>
      <%= button_to "Confirmar Exclusão", user_path(@user), 
          method: :delete, 
          class: "btn btn-danger",
          form: { id: "delete-form" } %>
    </div>
  </div>
</div>

<script>
  function showDeleteModal() {
    document.getElementById('delete-account-modal').style.display = 'flex';
  }
  
  function closeDeleteModal() {
    document.getElementById('delete-account-modal').style.display = 'none';
  }
  
  // Fechar modal ao clicar no overlay
  document.getElementById('delete-account-modal').addEventListener('click', function(e) {
    if (e.target === this) {
      closeDeleteModal();
    }
  });
  
  // Fechar modal com tecla ESC
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
      closeDeleteModal();
    }
  });
</script>
