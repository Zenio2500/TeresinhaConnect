<div class="mb-4">
  <h1>Leitores</h1>
  <p style="color: var(--gray-600);">Gerencie os leitores para as celebrações litúrgicas</p>
</div>

<div class="card">
  <div class="card-header">
    <h3 class="card-title">Todos os Leitores (<%= @readers.count %>)</h3>
    <div class="btn-group">
      <%= link_to "➕ Novo Leitor", new_reader_path, class: "btn btn-primary" %>
      <% liturgia_pastoral = Pastoral.find_by("LOWER(name) = ?", "liturgia") %>
      <% if liturgia_pastoral %>
        <%= link_to "Voltar", pastoral_path(liturgia_pastoral), class: "btn btn-secondary" %>
      <% end %>
    </div>
  </div>
  
  <div class="card-body p-0">
    <% if @readers.any? %>
      <div class="table-container">
        <table class="table">
          <thead>
            <tr>
              <th>Usuário</th>
              <th>Disponibilidade</th>
              <th>Tipos de Leitura</th>
              <th class="text-center">Ações</th>
            </tr>
          </thead>
          <tbody>
            <% @readers.each do |reader| %>
              <tr>
                <td><strong><%= reader.user&.name || 'N/A' %></strong></td>
                <td>
                  <% if reader.disponibility.present? %>
                    <%= reader.disponibility.join(", ") %>
                  <% else %>
                    <span class="badge badge-secondary">Não especificada</span>
                  <% end %>
                </td>
                <td>
                  <% if reader.read_types.present? %>
                    <%= reader.read_types.join(", ") %>
                  <% else %>
                    <span class="badge badge-secondary">Não especificado</span>
                  <% end %>
                </td>
                <td class="text-center">
                  <div class="btn-group">
                    <%= link_to "Ver", reader_path(reader), class: "btn btn-sm btn-secondary" %>
                    <%= link_to "Editar", edit_reader_path(reader), class: "btn btn-sm btn-warning" %>
                    <button type="button" class="btn btn-sm btn-danger" onclick="showDeleteReaderModal<%= reader.id %>()">Excluir</button>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <p style="color: var(--gray-600); text-align: center; padding: var(--spacing-xl);">
        Nenhum leitor cadastrado ainda.
      </p>
    <% end %>
  </div>
</div>

<!-- Modais de Confirmação de Exclusão -->
<% @readers.each do |reader| %>
  <div id="delete-reader-modal-<%= reader.id %>" class="modal-overlay" style="display: none;">
    <div class="confirmation-modal">
      <div class="modal-header">
        <span class="modal-icon danger">🗑️</span>
        <h3 class="modal-title">Confirmar Exclusão de Leitor</h3>
      </div>
      <div class="modal-body">
        <p>Tem certeza que deseja excluir o leitor <strong><%= reader.user&.name || 'N/A' %></strong>?</p>
        <p>Esta ação não pode ser desfeita.</p>
      </div>
      <div class="modal-actions">
        <button class="btn btn-secondary" onclick="closeDeleteReaderModal<%= reader.id %>()">Cancelar</button>
        <%= button_to "Confirmar Exclusão", reader_path(reader), 
            method: :delete, 
            class: "btn btn-danger" %>
      </div>
    </div>
  </div>
<% end %>

<script>
  <% @readers.each do |reader| %>
    function showDeleteReaderModal<%= reader.id %>() {
      document.getElementById('delete-reader-modal-<%= reader.id %>').style.display = 'flex';
    }
    
    function closeDeleteReaderModal<%= reader.id %>() {
      document.getElementById('delete-reader-modal-<%= reader.id %>').style.display = 'none';
    }
    
    document.getElementById('delete-reader-modal-<%= reader.id %>').addEventListener('click', function(e) {
      if (e.target === this) {
        closeDeleteReaderModal<%= reader.id %>();
      }
    });
  <% end %>
  
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
      document.querySelectorAll('.modal-overlay').forEach(modal => {
        modal.style.display = 'none';
      });
    }
  });
</script>
