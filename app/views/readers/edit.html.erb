<div class="mb-4" style="display: flex; justify-content: space-between; align-items: center;">
  <div>
    <h1>Editar Leitor</h1>
    <p style="color: var(--gray-600);">Atualizar informações do leitor</p>
  </div>
  <button type="button" class="btn btn-danger" style="height: fit-content;" onclick="showDeleteReaderModal()">
    🗑️ Excluir Leitor
  </button>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", reader: @reader %>
  </div>
</div>

<!-- Modal de Confirmação de Exclusão -->
<div id="delete-reader-modal" class="modal-overlay" style="display: none;">
  <div class="confirmation-modal">
    <div class="modal-header">
      <span class="modal-icon danger">🗑️</span>
      <h3 class="modal-title">Confirmar Exclusão de Leitor</h3>
    </div>
    <div class="modal-body">
      <p>Tem certeza que deseja excluir o leitor <strong><%= @reader.user.name %></strong>?</p>
      <p>Esta ação não pode ser desfeita.</p>
    </div>
    <div class="modal-actions">
      <button class="btn btn-secondary" onclick="closeDeleteReaderModal()">Cancelar</button>
      <%= button_to "Confirmar Exclusão", reader_path(@reader), 
          method: :delete, 
          class: "btn btn-danger" %>
    </div>
  </div>
</div>

<script>
  function showDeleteReaderModal() {
    document.getElementById('delete-reader-modal').style.display = 'flex';
  }
  
  function closeDeleteReaderModal() {
    document.getElementById('delete-reader-modal').style.display = 'none';
  }
  
  document.getElementById('delete-reader-modal').addEventListener('click', function(e) {
    if (e.target === this) {
      closeDeleteReaderModal();
    }
  });
  
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
      closeDeleteReaderModal();
    }
  });
</script>
