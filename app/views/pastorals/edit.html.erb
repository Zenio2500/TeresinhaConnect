<div class="mb-4" style="display: flex; justify-content: space-between; align-items: center;">
  <div>
    <h1>Editar Pastoral</h1>
    <p style="color: var(--gray-600);">Atualizar informações da pastoral <%= @pastoral.name %></p>
  </div>
  <%= button_to pastoral_path(@pastoral), method: :delete, 
      class: "btn btn-danger", 
      style: "height: fit-content;",
      data: { confirm: "Tem certeza que deseja excluir esta pastoral? Esta ação não pode ser desfeita." } do %>
    🗑️ Excluir Pastoral
  <% end %>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", pastoral: @pastoral %>
  </div>
</div>
